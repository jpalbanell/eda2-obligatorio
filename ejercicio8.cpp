#include <cassert>
#include <string>
#include <iostream>
#include <limits>

using namespace std;

int main()
{
    // TODO
    int N;
    cin >> N;
    int* numeros = new int[N];
    for (int i = 0; i < N; i++) cin >> numeros[i];
    

    return 0;
}